# 前端功能現況審計報告

**時間**：2026-05-03  
**執行**：美金  
**用途**：供黃金審計，規劃後端功能實作順序

---

## 一、頁面路由總覽

| URL | 狀態 | 說明 |
|-----|------|------|
| `/` → `/dashboard` | ✅ | 自動重導向 |
| `/login` | ✅ | 登入頁（店家ID + Email + 密碼） |
| `/dashboard` | ✅ | 主儀表板 |
| `/customers` | ✅ | 客人列表 |
| `/customers/new` | ✅ | 新增客人表單 |
| `/customers/[id]` | ✅ | 客人詳細頁 |
| `/customers/[id]/edit` | ❌ **404** | 頁面存在連結但未建立 |
| `/customers/[id]/pets/new` | ✅ | 新增寵物表單 |
| `/customers/[id]/pets/[petId]` | ✅ | 寵物詳細頁 |
| `/customers/[id]/pets/[petId]/edit` | ❌ **404** | 頁面存在連結但未建立 |
| `/customers/[id]/pets/[petId]/grooming/new` | ✅ | 新增美容完工紀錄 |
| `/appointments` | ✅ | 預約排程（日期導航） |
| `/appointments/new` | ✅ | 新增預約表單 |
| `/boarding` | ✅ | 住宿管理 |
| `/reports` | ✅ | 營收報表 |
| `/settings` | ✅ | 系統設定 |
| `/contract/[token]` | ✅ | 公開合約簽署頁（無需登入） |
| `/grooming/[token]` | ✅ | 公開美容完工通知頁（無需登入） |

---

## 二、各頁面功能細節

### 📊 儀表板 `/dashboard`
- 4 張統計卡：今日預約數、住宿中寵物數、本月收入、總客人數
- 近 6 個月收入 BarChart（recharts）
- 住宿中寵物列表（最多 5 筆）
- 今日預約表格（時間、寵物/客人、服務、美容師、狀態、費用）

### 👥 客人管理 `/customers`
- 搜尋欄（姓名/電話/LINE ID）
- 客人卡片列表（頭像、姓名、會員等級彩色標籤、電話、LINE、儲值金額、點數、美容次數、上次到店）
- 每隻寵物的合約狀態 badge

### 📝 新增客人 `/customers/new`
- 欄位：姓名*、電話*、LINE ID、地址、備註
- POST → `/api/customers`，成功後跳客人詳情

### 👤 客人詳情 `/customers/[id]`
- 頂部：基本資訊 + 編輯按鈕（連結 **尚未實作**）
- 4 張統計卡：儲值餘額、點數、寵物數量、累計美容次數
- **Tabs 四分頁**：
  - **寵物列表**：卡片顯示每隻寵物（合約狀態、美容次數、上次到店）+ 新增寵物按鈕
  - **付款紀錄**：表格（時間、金額、方式、備註）
  - **點數紀錄**：表格（時間、點數異動、原因）—— 顏色區分正負
  - **儲值紀錄**：表格（時間、金額異動、原因）—— 顏色區分正負

### 🐾 新增寵物 `/customers/[id]/pets/new`
- 欄位：名稱*、物種（犬/貓/兔/鳥/其他）、品種、性別、生日、晶片號碼、特殊疾病、過敏紀錄、備註
- POST → `/api/pets`

### 🐾 寵物詳情 `/customers/[id]/pets/[petId]`
- 麵包屑導覽
- 頭部：物種表情符號 + 名稱/品種/性別/年齡（自動計算）
- 4 張統計卡：合約狀態、美容次數、上次到店日、住宿次數
- **Tabs 五分頁**：
  - **基本資料**：物種/品種/性別/生日/年齡/晶片 grid + 特殊疾病/過敏（紅色警告）/備註
  - **合約**：合約狀態（已簽/待簽/過期）+ 複製簽署連結功能
  - **美容紀錄**：卡片列表（日期、美容師、費用、服務 badges、皮膚/毛況/備註）+ 完工報告連結 + 新增按鈕
  - **住宿紀錄**：卡片列表（狀態、房間、入住退房日、費用）
  - **預約**：卡片列表（狀態、時間、服務、費用）

### ✂️ 新增美容紀錄 `/customers/[id]/pets/[petId]/grooming/new`
- 日期、美容師（下拉，從 API 取）
- 服務項目：從下拉選擇服務 + 手動輸入名稱 + 金額，可新增/刪除多行，自動加總
- 使用產品、皮膚狀況、毛況、備註
- POST → `/api/grooming`

### 📅 預約排程 `/appointments`
- 日期導航（前後一天 + 回今天）
- 預約卡片列表：時間（彩色狀態圓點）、寵物/客人、服務、美容師、狀態 badge、費用
- 每筆預約可直接變更狀態（下拉 select）
- 新增預約按鈕 → `/appointments/new`

### 📅 新增預約 `/appointments/new`
- 客人搜尋（即時下拉，含姓名/電話）
- 寵物下拉（依選定客人篩選）
- 日期時間選擇器（預設今日 10:00）
- 預約類型（美容/住宿/諮詢）、預計時長、來源（現場/電話/LINE/網路）
- 負責美容師下拉
- 服務選擇：分類顯示可點選 pill，自動計算費用＋時長
- 預估費用（可手動覆蓋）
- 備註
- POST → `/api/appointments`

### 🏠 住宿管理 `/boarding`
- 統計：住宿中數量 + 空房數量
- **房間狀態格（Grid）**：綠色=空房、橘色=使用中、紅色=維修中，顯示住宿寵物名稱/客人/日費
- **住宿中寵物列表**：姓名/客人、房間、入住日、第幾天、累計費用 + 辦理退房按鈕
- **近期退房紀錄**：表格（寵物/客人、房間、入住日、退房日、費用）
- 新增住宿 Dialog 按鈕

### 📈 營收報表 `/reports`
- 4 張統計卡：本月收入、本月筆數、平均客單、服務種類數
- 近 6 個月收入趨勢 BarChart
- 本月付款方式分佈（橫向進度條 + 筆數/金額）
- 本月每日收入 AreaChart
- 本月服務項目業績排行表（服務名、次數、收入）

### ⚙️ 系統設定 `/settings`
- **Tabs 四分頁**：
  - **服務項目**：列表 + 新增/刪除，欄位：名稱、分類、價格、時長（分鐘）
  - **住宿房間**：列表 + 新增/刪除，欄位：名稱、類型、日費
  - **會員等級**：列表 + 新增/刪除，欄位：名稱、最低點數門檻、折扣率、權益說明、顏色
  - **合約範本**：HTML textarea 編輯器 + 儲存（PATCH → `/api/shops/[shopId]`）

### 📜 合約簽署 `/contract/[token]`（公開頁，無需登入）
- 已簽署：顯示完成畫面（簽署時間/人名）
- 已過期：顯示錯誤提示
- 待簽署：
  - 寵物資訊 banner
  - 合約全文（HTML 渲染）
  - 手繪簽名板（finger/mouse，react-signature-canvas）
  - 簽署人姓名輸入
  - 同意勾選框 + 確認簽署按鈕
  - POST → `/api/contracts/[id]/sign`

### 📸 美容完工通知 `/grooming/[token]`（公開頁，無需登入）
- 標題 header（剪刀 icon + 完工日期）
- 寵物名稱/品種/客人 + 「美容完成！」徽章
- 服務項目列表 + 總計費用
- 寵物今日狀況：使用產品、皮膚狀況、毛況、美容師備註
- 服務美容師資訊

---

## 三、API 路由總覽

| 端點 | 方法 | 說明 |
|------|------|------|
| `/api/auth/[...nextauth]` | GET/POST | NextAuth 認證 |
| `/api/dashboard` | GET | 儀表板統計（UI 未使用，頁面直接查 DB） |
| `/api/customers` | GET/POST | 客人列表/新增 |
| `/api/customers/[id]` | GET/PUT/DELETE | 客人詳情/修改/刪除 |
| `/api/pets` | GET/POST | 寵物列表/新增 |
| `/api/pets/[id]` | GET/PATCH/DELETE | 寵物詳情/修改/刪除 |
| `/api/pets/all` | GET | 全店寵物列表（含客人名） |
| `/api/contracts/[id]/sign` | POST | 合約簽署（含 canvas 簽名） |
| `/api/grooming` | GET/POST | 美容紀錄列表/新增 |
| `/api/appointments` | GET/POST | 預約列表/新增 |
| `/api/appointments/[id]` | PATCH/DELETE | 預約狀態更新/刪除 |
| `/api/boarding` | GET/POST | 住宿紀錄列表/新增 |
| `/api/boarding/[id]` | PATCH | 住宿退房更新（計算費用） |
| `/api/services` | GET/POST | 服務項目列表/新增 |
| `/api/rooms` | GET/POST | 房間列表/新增 |
| `/api/staff` | GET | 員工列表 |
| `/api/shops/[shopId]` | PATCH | 店家設定更新（合約範本等） |

---

## 四、審計發現：缺口清單

### ❌ 404 頁面（UI 有連結但頁面不存在）

1. `/customers/[id]/edit` — 客人詳情頁有「編輯」按鈕
2. `/customers/[id]/pets/[petId]/edit` — 寵物詳情頁有「編輯寵物資料」按鈕

### ⚠️ 後端功能有 API 但前端無操作入口

1. **付款記錄** — 無建立付款的 UI（美容完工後如何結帳？）
2. **點數異動**（`PointsHistory`）— 無手動加/扣點數的 UI
3. **儲值加值**（`StoredValueHistory`）— 無儲值充值的 UI
4. **月租方案**（`MonthlyPlan`）— 設定頁無月租管理，客人頁也無綁定 UI
5. **服務停用** — 設定頁無停用功能，只有刪除（schema 有 `isActive` 欄位）
6. **合約建立時機** — 新增寵物後應自動建立合約，目前 `/api/pets` POST 未確認是否自動建立

### ⚠️ API 存在但後端邏輯尚未完整接上

1. `/api/dashboard` route.ts 存在但 Dashboard 頁面直接查 Prisma，API 被閒置
2. `PATCH /api/customers/[id]` — 有 API 但 edit 頁面不存在
3. `PATCH /api/pets/[id]` — 有 API 但 edit 頁面不存在
4. 美容完工後沒有自動建立 `Payment` 紀錄的流程

### ⚠️ 需求文件有但尚未實作

1. **疫苗紀錄** — schema 有 `vaccineRecords` 欄位（JSON 陣列），但新增/顯示 UI 皆無
2. **照片上傳** — 美容前後照（`beforePhotoUrl`, `afterPhotoUrl`）、寵物照（`photoUrl`）無上傳 UI
3. **PDF 合約下載** — schema 有 `pdfUrl`，但未實作 PDF 生成
4. **LINE 預約連結** — 無 `/booking/[shopId]` 公開預約頁
5. **預約轉美容紀錄** — 預約完成後一鍵建立美容紀錄的流程不存在
6. **月租方案管理** — 設定頁 `monthlyPlans` 資料有取但無 UI Tab 顯示
7. **客人編輯 / 寵物編輯** — edit 頁面未建立

---

## 五、優先順序建議（供黃金規劃）

### P1 — 高優先（核心流程缺口）
| # | 功能 | 說明 |
|---|------|------|
| 1 | 合約自動建立 | 新增寵物 → `/api/pets` POST 自動建立對應合約 |
| 2 | 美容完工結帳流程 | 建立美容紀錄 → 選付款方式 → 建立 Payment + 扣儲值/加點數 |
| 3 | 客人編輯頁 | `/customers/[id]/edit` |
| 4 | 寵物編輯頁 | `/customers/[id]/pets/[petId]/edit` |

### P2 — 中優先（常用輔助功能）
| # | 功能 | 說明 |
|---|------|------|
| 5 | 儲值加值 UI | 客人詳情頁新增充值操作 |
| 6 | 點數手動調整 UI | 客人詳情頁新增加/扣點操作 |
| 7 | 疫苗紀錄 UI | 寵物詳情頁顯示/編輯疫苗資訊 |
| 8 | 月租方案管理 | 設定頁新增 Tab + 客人綁定月租 |

### P3 — 低優先（進階功能）
| # | 功能 | 說明 |
|---|------|------|
| 9 | PDF 合約生成 | 簽署完成後產生 PDF 下載 |
| 10 | 照片上傳 | 美容前後照 + 寵物頭貼 |
| 11 | LINE 公開預約頁 | `/booking/[shopId]` 讓客人自助預約 |
| 12 | 預約轉美容紀錄 | 預約頁一鍵建立美容完工紀錄 |

---

*報告完畢，交黃金審計規劃。美金待命。*
